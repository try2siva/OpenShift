
Shortcut: ns -namespace, svc -Service, ds -daemonSet, deploy -deployments, rs -replicaSet, sts -statefulSets, no -nodes, cm -configMaps, cj -cronjob, ev -events, pv -persistentvolume, pvc -persistentvolumeclaim, sc -Storageclass and po -Pod
Taints(node) and Tolerations (pod)
Node Affinity (node)
Daemon Sets
static pods
Init containers


Taints create: #k taint node node01 env=prod:NoSchedule 
Taints remove: ##k taint node node01 env=prod:NoSchedule-

Toleration:- Define under the pod.yaml file under spec: location.
tolerations:
- key: "env"
  operator: "Equal"
  value: "UAT"
  effect: "NoSchedule"

NodeAffinity:
Add a label env=prod for the node01
Create a chn pod with nodeaffinity "requireDuringSchedulingIgnoreDuringExecution" and labels of env=prod check whether use Pod is scheduled to a node?

DaemonSet
It is run on all the worker pod with name of a daemonpod and it is helping monitor and logging. One Daemonpod-DP run on each workernode.
